{
  "name": "elocuency",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "bridge:build": "pnpm --filter elo-mac-bridge run build",
    "bridge:start": "pnpm --filter elo-mac-bridge run start",
    "dev": "pnpm -r run dev",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
{
  "name": "elocuency",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "bridge:build": "pnpm --filter elo-mac-bridge run build",
    "bridge:start": "pnpm --filter elo-mac-bridge run start",
    "dev": "pnpm -r run dev",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "test:cov": "pnpm -r run test:cov",
    "typecheck": "pnpm -r run typecheck",
    "install-available-apps": "node core/scripts/install-available-apps.js",
    "install-app": "node core/scripts/install-app.js",
    "sync-workspace": "node core/scripts/sync-workspace.js",
    "init-mcps": "node core/scripts/sync-workspace.js",
    "elo": "tsx apps/elo-cli/src/index.ts"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.18",
    "typescript": "^5.4.5",
    "vitest": "^4.0.18"
  }
}